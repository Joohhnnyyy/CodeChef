#include <bits/stdc++.h>
using namespace std;

int main() {
    // your code goes here
    int times;
    cin >> times;
    while( times-- ){
        int n ;
        cin >> n;
        vector<int>arr(n);
        for(int j = 0 ; j < n ; j++)cin>> arr[j];
        int maximun = arr[0];
        for(auto i ){
            if(maximum < arr[i])maximum = arr[i];
        }cout << maximum << endl;
    }
}

if local
int local
include local
if keyword
inline keyword
int keyword
int16_t keyword
int32_t keyword
int64_t keyword