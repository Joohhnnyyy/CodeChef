#include <bits/stdc++.h>
using namespace std;

int main() {
    // your code goes here
    int times;
    cin >> times;
    while( times-- ){
        int n ;
        cin >> n;
        vector<int>arr(n);
        for(int i = 0 ; i < n ; i++)cin>> arr[i];
        int maximun = arr[0];
        for(int i = 0 ; i < arr.size() ; i++){
            if(maximum < arr[i])maximum = arr[i];
        }cout << maximum 
    }
}

maximum local
maximun local
main local
make_heap snippet
map snippet
max snippet
max_element snippet
mmap snippet
umap snippet